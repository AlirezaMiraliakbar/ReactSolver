from ReactSolver.reactor import ReactorSolver

def test_reactor_solver():
    solver = ReactorSolver(params={"some_param": 1.0})
    # Add assertions to test the solver's functionality
    assert solver is not None
